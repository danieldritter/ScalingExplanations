{
    "dataset_name":"eraser",
    "dataset_kwargs":{"subset":"cose"},
    "finetuning":false,
    "pretrained_model_name":"bert-cls-seq-class",
    "pretrained_model_config":"bert-base-uncased",
    "tokenizer_config_name":"bert-base-uncased",
    "seq2seq":false,
    "num_labels":5,
    "test_split":"test",
    "trained_layers": ["classifier.dense.weight", "classifier.dense.bias", "classifier.out_proj.weight", "classifier.out_proj.bias"]
}